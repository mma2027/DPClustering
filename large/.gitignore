# Experiment outputs and scratch — regenerated by the run scripts, not version-controlled.
# Only the scripts + EXPERIMENT_PLAN.md in this directory are tracked.
accuracy/
timing/
parts/
logs/
.status/
.dispatch/
*.log
